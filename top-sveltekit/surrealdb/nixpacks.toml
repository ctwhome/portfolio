[phases.setup]
nixPkgs = [
    'bun',
    'gcc',
    'glib',
    'pkg-config',
    'vips',
    'stdenv.cc.cc.lib'
]

[phases.install]
cmds = ['bun install --include=optional']

[phases.build]
cmds = ['bun run build']

[start]
cmd = "bun run start"
